<% if line_item == @current_item %>
<tr id="current_item" >
<% else %>
<tr>
<% end %>  
  <%#returned value = destroy(@line_item.quantity); %>
  <td><%= line_item.quantity %>&times;</td>
  <td><%= line_item.product.title %> </td>
  <td class="item_price"><%= number_to_currency(line_item.total_price) %></td>
  <td> <%= button_to 'Decrement Item', line_item, :method => :delete, :confirm => "Are you sure?", :remote => true %> </td>

</tr>